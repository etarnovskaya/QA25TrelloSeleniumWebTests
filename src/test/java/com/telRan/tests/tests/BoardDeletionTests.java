package com.telRan.tests.tests;

import org.testng.annotations.Test;

public class BoardDeletionTests extends TestBase {
    @Test
    public void testBoardDeletion() throws InterruptedException {
//        int before = app.board().getBoardsCount();
//        while(before>5) {
            app.board().openFirstPersonalBoard();
//            app.board().clickMoreButton();
//            app.board().initBoardDeletionInMoreMenu();
//
//            app.board().permanentlyDeleteBoard();
//            app.header().returnOnHomePageAfterBoardDeleted();
//            before = app.board().getBoardsCount();
//        }
    }
}
